# Nixpacks 配置（项目根目录）
# start 用 npx 跑 pnpm

[phases.setup]
nixPkgs = ["nodejs_20"]

[start]
cmd = "cd server && npx -y pnpm@10 prisma db push --accept-data-loss --schema=./prisma/schema.postgres.prisma && node dist/server.js"
