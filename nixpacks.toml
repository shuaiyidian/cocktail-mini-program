# Nixpacks 配置（项目根目录）
# pnpm 通过 build command 装

[phases.setup]
nixPkgs = ["nodejs_20"]

[start]
cmd = "cd server && pnpm prisma db push --accept-data-loss --schema=./prisma/schema.postgres.prisma && node dist/server.js"
